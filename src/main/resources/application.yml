server:
  port: 8080
  
spring:
  datasource:
    url: jdbc:h2:mem:health
    username: root
    password: root
    
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create